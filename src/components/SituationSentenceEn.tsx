import styled from "styled-components";
import tw from "twin.macro";

const SituationSentenceEn = styled.div`
  ${tw`font-semibold text-sm md:text-xl tracking-wide font-open-sans italic`}
`;

export default SituationSentenceEn;
