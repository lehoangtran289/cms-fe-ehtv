import styled from "styled-components";
import tw from "twin.macro";

const SituationSentenceVi = styled.div`
  ${tw`font-semibold text-base md:text-3xl tracking-wider`}
`;

export default SituationSentenceVi;
